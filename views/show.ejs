<% include ./partials/header %>

<div class="ui raised very padded text container segment">
    <div class="image">
        <img src="<%= blog.image %>">
    </div>
    <div class="content">
        <div class="header"><h1><%= blog.title %></div></h1>
        <div class="meta">
            <span class="date"><%= blog.created %></span>
        </div>
        <br>
        <div class="description">
            <%= blog.body %>
        </div>
    </div>
</div>

<% include ./partials/footer %>